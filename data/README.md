# Data notes

This directory will document dataset sources, splits, preprocessing, and any external download instructions.

The first release will avoid committing large raw datasets directly unless license and size constraints allow it.
