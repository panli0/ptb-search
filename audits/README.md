# Audit artifacts

This directory will contain completeness, leakage, duplicate, and consistency checks.

Reported checks include:

- expected vs observed problem/cell counts;
- expected vs observed proposal attempts;
- duplicate candidate-id checks;
- scoring-error checks;
- verification that OOD/test/true-formula/oracle information is not used for fitting, selection, ranking, or term-bank construction.
