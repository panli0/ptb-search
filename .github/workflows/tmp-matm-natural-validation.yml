name: tmp-matm-natural-validation

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'MATM natural-validation runner archived after corrected v0.2 + final v0.3 sensitivity audit.'
